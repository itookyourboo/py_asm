section .data
    NULL_TERM: 0x00
    HELLO: "hello world\n"

section .text
    .print_char:
        LD %rdx, #HELLO[%rdi]
        PUTC %rdx
        CMP %rdx, #NULL_TERM
        JE .exit
        INC %rdi
        JMP .print_char
    .exit:
        HLT
